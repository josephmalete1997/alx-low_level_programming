#include "main.h"
/**
 * add - function computes the sum of a and b
 * @a: first number for computation
 * @b: second number for computation
 * Return: results
 */
int add(int a, int b)
{
	int results;

	results = a + b;
	return (results);
}
