#include "main.h"

/**
 * _islower(int c) - checks if c is lowercase and return 1 else return 0
 */

int _islower(int c)
{
	c = _islower('j');
	if (_islower(c)){
		_putchar(1);
		return (1);
	}
	else
	{
		_putchar(0);
		return (0);
	}
}
